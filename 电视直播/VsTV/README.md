## 下载与安装

每个版本通常包含 **三个** 安装包，请按需选择：

| 文件                                      | 适用说明                                                     |
| ----------------------------------------- | ------------------------------------------------------------ |
| **`vstv-<版本>-all-sdk21.apk`**           | **默认选择**：桌面显示 **VsTV**，包名 `com.vesaa.mytv`，适合大多数电视 / 盒子 / 手机。 |
| **`vstv-<版本>-all-sdk21-lite.apk`**      | **低配 / 内存紧张** 时使用：**仅关闭频道台标**（不加载台标图、不初始化台标缓存），**其余功能与标准版一致**。适用于 **S915L、RK3566 及同档或更低** 的 CPU，以及 **电信 / 移动等运营商赠送的低端盒子**（易因台标解码与缓存叠加导致卡顿或闪退）。 |
| **`vstv-<版本>-all-sdk21-HarmonyOS.apk`** | **华为智慧屏 / 需不同包名** 时使用：桌面名 **Z视介**，包名 `com.chinablue.tv`，与上表 **数据与更新通道不互通**，请勿混用升级包。 |

应用内 **检查更新** 会按当前安装类型拉取 **对应附件**：标准版对应 **`…-all-sdk21.apk`**，lite 对应 **`…-all-sdk21-lite.apk`**。若安装的是 **HarmonyOS** 变体，请到 Release 页下载带 **HarmonyOS** 后缀的包升级（与常规包名不同，勿混装）。

## 使用说明（摘要）

**遥控器**：上下换台，左右切多线路，OK 确认，菜单/帮助/长按 OK 或双击屏幕打开设置等（与常见视频应用习惯接近）。

**触屏**：滑动模拟方向键，点击为确认，长按 / 双击对应长按 OK / 菜单。

**设置**：在播放界面按上述手势进入；

**关于** 位于设置网格最后一项，内含本机 **网页配置地址**、**切换扫码用 IP** 与 **二维码**。

**网页配置**：浏览器打开 **`http://<电视IP>:1616`**，可修改直播源、节目单等（与电视端设置写入同一配置）。
